/**
 * Altura da barra superior (logo na sidebar + header).
 * Mesmo valor nos dois para as linhas divisórias ficarem alinhadas.
 */
export const BAR_HEIGHT = '16';

/** Largura da sidebar expandida (Chakra space unit). */
export const SIDEBAR_WIDTH_EXPANDED = 60;

/** Largura da sidebar recolhida (Chakra space unit). */
export const SIDEBAR_WIDTH_COLLAPSED = 20;

/** Tamanho dos ícones na sidebar (recolhida e expandida). */
export const SIDEBAR_ICON_SIZE = '16';

/** Tamanho da fonte dos textos na sidebar (logo e itens de navegação). */
export const SIDEBAR_FONT_SIZE = 'sm';

/** Altura fixa do hover/item dos itens de nav e do toggle (recolhida e expandida). */
export const SIDEBAR_NAV_ITEM_H = '9';

/** Recuo horizontal (margin/padding) em % para mesma proporção do hover recolhida e expandida. */
export const SIDEBAR_HORIZONTAL_INSET = '10%';
